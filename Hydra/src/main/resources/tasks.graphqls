type Query {
    allTasks(uncompletedOnly: Boolean = true): [Task]
}

type Task {
    id: String
    description: String
    completed: Boolean
}

type Mutation {
    complete(id: String!): Boolean
}
